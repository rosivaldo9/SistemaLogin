/*const User = require('../src/model/user')
const httpMocks = require('node-mocks-http')
const UserControler = require('../src/Controller/authController')
const newUser = require('./User.json')


let req, res, next;

beforeEach(()=>{
    req = httpMocks.createRequest();
    req = httpMocks.createResponse();
    next = null;
})

describe('Iniciando teste dos controller de usuario', ()=>{
    beforeEach(()=>{
        req.body = newUser;
    })

    instanceof('Testando função createUser', ()=>{
        UserControler.cre
    })
})*/